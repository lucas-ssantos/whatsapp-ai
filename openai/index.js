// Exporta todas as funcionalidades da pasta OpenAI
const OpenAIClient = require('./openai-client');
const ConversationManager = require('./conversation-manager');

module.exports = {
  OpenAIClient,
  ConversationManager
};